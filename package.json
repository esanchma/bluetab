{
  "name": "@esanchma/bluetab",
  "version": "0.1.3",
  "description": "Minimal MV3 brotab-like tab CLI for Chromium browsers.",
  "type": "module",
  "bin": {
    "bt": "cli/bt.js"
  },
  "files": [
    "cli/",
    "src/",
    "scripts/",
    "extension/",
    "native-host.example.json",
    "README.md"
  ],
  "scripts": {
    "sync:version": "node ./scripts/sync-version.js",
    "check": "bun run sync:version && tsc --noEmit",
    "build": "bun run sync:version && bun build ./src/main.ts --compile --outfile ./dist/bt",
    "pack:extension": "bun run sync:version && node ./scripts/pack-extension.js",
    "prepack": "bun run check"
  },
  "keywords": [
    "brotab",
    "browser-tabs",
    "chromium",
    "brave",
    "native-messaging",
    "mv3"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "bun": "^1.3.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
